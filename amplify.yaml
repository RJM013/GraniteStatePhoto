version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build commands required for static site"
    build:
      commands:
        - echo "Build completed"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []